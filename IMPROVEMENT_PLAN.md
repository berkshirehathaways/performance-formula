# Compass Fork Improvement Plan

## 0. Guardrail (원래 구현 의도 보존)
- 진단 우선 원칙 유지: Step 3의 순차 진단을 우회하지 않는다.
- Lock 유지: 9요소 완료 전 Step 6 생성 불가.
- 자동 진행 금지: 모든 주요 전환은 사용자 클릭으로 진행.
- 설명 과잉 금지: 이론 백과사전이 아니라 실행 도구로 유지.

## 1. 반영한 개선 (이번 포크)
- 완성 예시 미리보기 추가 (Step 0)
- 저장 신뢰 신호 강화: 헤더에 마지막 자동저장 시각 표시
- Step 3에서 목표(북극성/goal) 상시 노출
- 계획 기간 프리셋 추가: 14/30/60/100일 (기본 100일 유지)
- 기간에 맞춰 Phase 일자/주차 자동 재계산
- 분석 버그 수정: unknown 분류 시 실제 사용자 reflection 사용
- 계획 잠금 안전성 보강: safe generator 경유
- 출력 이스케이프 일부 보강(signals/practices)
- CSS 토큰 오류 수정(var(--font-sans) -> var(--font))

## 2. Devil's Advocate Review
### A. 예시 미리보기는 오히려 편향을 만들지 않는가?
- 리스크: 사용자가 예시를 정답처럼 따라가 자기 목표 탐색이 약해질 수 있음.
- 대응: 예시는 "미리보기"로만 제공, 자동 입력/자동 완료는 제공하지 않음.

### B. 기간 가변은 원래 100일 철학을 약화시키지 않는가?
- 리스크: 브랜드 메시지(100일 여정)가 희석될 수 있음.
- 대응: 기본값을 100일로 고정하고, 단축 옵션은 학습/입문 사용자 진입 장벽 완화용으로 제공.

### C. 저장 신호 강화가 실제 안정성을 보장하는가?
- 리스크: 로컬 저장 한계를 사용자가 오해할 수 있음.
- 대응: "현재 브라우저 저장" 문구를 명시. 추후 export/import 또는 로그인 저장을 별도 단계로 분리.

### D. 목표 상시 노출이 화면 집중도를 해치지 않는가?
- 리스크: Step 3의 요소 집중도가 떨어질 수 있음.
- 대응: 1줄 스트립 형태로 최소 정보만 노출.

## 3. 2중/3중 검토 체크리스트
### 1차: 기능 검토
- [ ] Step 3 순차 플로우가 그대로 유지되는가?
- [ ] 9요소 미완료 시 Step 6 접근이 차단되는가?
- [ ] 기간 프리셋 변경 시 Step 6/7 내용이 동기화되는가?

### 2차: 의도 검토
- [ ] "진단 우선" 메시지가 약화되지 않았는가?
- [ ] 사용자에게 과도한 설명을 강요하지 않는가?
- [ ] 예시는 동기 부여용으로만 작동하는가?

### 3차: 신뢰/안전 검토
- [ ] 저장 상태가 사용자에게 명확히 보이는가?
- [ ] 사용자 입력 렌더링 시 HTML 주입 위험이 없는가?
- [ ] 로컬 저장 한계(기기/브라우저 종속)가 안내되는가?

## 4. 다음 단계 (미반영)
- Supabase Auth + 사용자별 저장 (옵션)
- JSON Export/Import
- 퍼널 지표 수집 (Step 이탈률, 완주율, 재방문율)
- goalType/domain별 예시 확장 라이브러리
